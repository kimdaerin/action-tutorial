
name: password

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Password
        env:
          MY_PASSWORD: ${{ secret.PASSWORD }}
        run: echo My passowrd is $MY_PASSWORD
